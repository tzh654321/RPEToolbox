# 2026-09-13

## docs/ai提示词历史.txt → ai提示词历史.md（文档整理）

- 把 2461 行、77KB 的 AI 提示词历史（13 轮对话）整理为结构化 markdown `docs/ai提示词历史.md`（约 2690 行）。
- 结构：文首来源说明 + 13 项目录；每轮 `## 第 N 轮 · <主题>`；轮内短标签提升为 `###`；JSON 示例逐字保留为 ```json 代码块（共 22 段）；缩进正文转多级列表。
- 手工润色：`easingType` 1~29 对照表还原为 markdown 表格；`bezier` / `bezierPoints` 字段改为字段表；第 1 轮三条功能拆成 `###` 小标题。
- 内容零丢失：逐行归一化比对，22 处"未命中"全部是分隔线/被提升的标签/代码块起始行/刻意改写的段落，正文与 JSON 均与原文一致。原 txt 保留未删。
- 复用：通用化脚本与踩坑记录沉淀为 skill `prompt-history-to-md`（含 `convert.py`，支持 `--titles` / `--promote` 与自动零丢失校验）。踩坑：轮次内切片必须用当前轮的 body 而非全文行号；JSON 块判定要加"后续出现键名行"守卫，否则会把 `interface X {` 吃成 JSON。

## 需求十四：极坐标旋转不切割 + 文案外置 + DPI + sv-ttk

**功能改动**
- `func_polar_conversion`：旋转事件（type 3）不再按密度切割重采样，改为 `copy.deepcopy` 原样复制输入（仍参与 r/θ 换算）；X/Y 仍按密度切割。删除了 `temp_events_rot` / `last_rot` 链路。
- 顺带修掉一处隐藏 bug：UI 下拉框存"5k(常规事件)/7k(包括缩放)"，而 core 比较 `hold_mode in ("5k","7k")` → 定轨 hold 从界面永远不生效。改为统一走 `i18n.option_key("hold_mode", 界面值)`。

**架构改动**
- 新增 `assets/lang/zh-CN.json`（纯数据、不含代码）：app/labels/buttons/options/functions/dialogs/errors 七组；`options.*` 与 `functions[].key` 是"内部键→显示文字"映射，键不可改、值可翻译。
- 新增 `rpe_toolbox/i18n.py`（t / option_values / option_key / option_int_key / function_list）、`dpi.py`、`theme.py`、`config.py`；`resources.py` 加 `LANG_DIR` / `lang_path` / `user_config_path`。
- `__init__.py` 改为惰性属性（PEP 562）：否则 `import rpe_toolbox` 会经 `.app` 提前引入 tkinter，抢在 `enable_dpi_awareness()` 之前。
- `RPET.py` 在 `import tkinter` 之前调用 `dpi.enable_dpi_awareness()`，建 root 后 `dpi.sync_tk_scaling()`（tk scaling = dpi/72）。

**外观**
- app.py 全量 tk 控件迁移到 ttk（`winfo_class` 变 TLabel/TButton），带硬编码颜色的三个按钮改 `Accent.TButton`；`tk.Text` 保留但由 `theme.PALETTES` 手动配色。
- 主题默认浅色、右上角「深色模式」开关可切换，持久化到 `%APPDATA%/RPEToolbox/config.json`，`RPET_THEME` 环境变量优先级最高；sv-ttk 缺失时静默降级。
- 高分屏：DPI 感知后 Tk 尺寸单位是物理像素、字号仍按点渲染，故新增 `self.px()` 把几何尺寸/内边距/换行宽度按缩放比放大（150% 下窗口 680×760 → 1020×1140），否则界面拥挤。功能说明文字从顶栏右侧移到独立一行。

**验证**（全部通过，共 155 项断言）
- 新增 `tests/_test_req14.py`（60 项）：A 旋转复制/仍参与换算、B 文案键完整+代码引用键存在、C AST 扫描确认代码内无残留中文界面文案（仅允许字体名与 i18n 引导报错）、D 导入 dpi 不引入 tkinter、E 主题归一化与环境变量/配置优先级、F 界面主题切换+DPI 缩放。
- 改动 `tests/_test_req12.py`：箭头统计兼容 TLabel。
- 踩坑：测试里切换主题会写真实用户配置（曾把我的机器启动主题改成深色），且 `os.remove` 在沙箱内被 safe-delete 拦截——测试改为文件顶部重定向 `APPDATA` 到临时目录并在结束时还原。
- 踩坑：截图核对主题时，切换后需多次 `update()+sleep` 才重绘，否则抓到的是旧的浅色渲染。

## 需求十五：标题栏 + 字体 + 按钮配色 + 功能介绍位置 + 输入框对比度 + Notebook 切换

**功能切换改为 Notebook**（`notebook.add(tab, text=...)`）
- `create_widgets` 里用 `ttk.Notebook` 取代原来的功能下拉框：8 个功能各一页，页内放该功能的选项；
  `_build_function_tabs()` 负责把各选项控件建到对应页里（`frame_hold_options` / `frame_event_convert` / `frame_image_settings` / `frame_time_offset_settings` / `frame_stretch_settings` / `frame_midi_settings` 都改了父容器）。
- 新增 `self.tab_frames`（key → Frame）、`self.tab_order`（下标 → key）、`self._syncing_tab` 防回环。
  `current_function` ↔ 标签页双向同步：`_select_function_tab()` 与 `_on_tab_changed()` 互相调用前都置标志位。
- 语言文件 functions 项新增 `tab` 短标签（首尾相接/非线性切割/极坐标转换/类型转换/图片转音符/间隔转y偏移/倒序/拉伸/BPM提取），8 个标签一行排得下；`name` 仍是完整名（测试与 `on_function_change` 用它）。
- 切割密度两页各一份控件、共用同一个 `density_var`（Tk 不允许给 widget 换父容器）。
- 标签页高矮不一：不做固定高度对齐（短页会留一大片空白），按当前页自然高度、由下方 `expand=True` 的文本框吸收。
- 窗口高度 760 → 800（设计像素），两个文本框各 9 行。

**标题栏**：`pywinstyles.apply_style(root, "dark"/"light")` 跟随主题，未安装时静默跳过。实测标题栏中段主色 浅色 (240,243,249) / 深色 (29,32,38) ✓。

**字体**：ttk 控件不读 `option_add("*Font")`，导致按钮/开关/下拉框退回 Tk 默认字体。
新增 `_apply_ui_font()`：对 `.`/TLabel/TButton/TCheckbutton/TCombobox/TEntry/TNotebook/TNotebook.Tab/Muted.TLabel/Switch.TCheckbutton 统一 `style.configure(..., font=(家族,10))`。本机取到"微软雅黑"。

**按钮配色**：三个按钮改回经典 `tk.Button` + 最初配色（#8EC990/#0C4E1A、#E6E7AB/#5A550E、#e6766e/#EEDFE5），配色表放进 `theme.BUTTON_COLORS`。
**重大坑**：sv-ttk 的 Tcl 主题在 `set_theme` 里调用 `tk_setPalette`，会把已存在的经典 tk 控件（按钮、Text）重新着色成主题色，且**推迟到下一次空闲/update 时执行**——`pywinstyles` 内部的 `update()` 正好触发了它，所以按钮颜色"设了却被改回去"。
修法：`_apply_theme` 里把 `_apply_titlebar_style()` 排在 `_apply_widget_colors()` 之前，并再挂一个 `after_idle` 双保险；`_apply_widget_colors()` 统一管按钮 + 文本框配色。**测试必须在 `update()` 之后断言 `cget('bg')`。**

**其他**：功能介绍回到顶部原位置（右对齐、`Muted.TLabel` 灰字），不再单独占一行；输入框对比度提升（浅色纯白底+#8a8a8a 描边、深色 #0e0e0e 底+#5a5a5a 描边，并加内部 padding）。

**验证**：新增 `tests/_test_req15.py`（57 项，A Notebook 切换/选项归属、B 字体、C 按钮配色含切主题后、D 简介灰字与位置、E 输入框对比度、F 标题栏降级）；`_test_req14.py` 因删除未使用的 `labels.select_function` 键同步改了一处断言。全套 9 个测试文件共 212 项断言全绿。
**踩坑**：typo/引用类改动要全量复跑（删掉一个文案键就让 req14 挂了一项）；`grep -c` 无匹配时退出码 1，会让 `set -e` 风格的循环提前结束，改用写日志再读。

## 需求十六：输入/按钮/输出移入标签页 + 圆角输入框 + 标签字号 + 更纱黑体

**圆角文本区**
- 新增 `rpe_toolbox/widgets.py` 的 `RoundedTextArea`：Frame → Canvas（`create_polygon(smooth=True)` 画圆角底+描边，聚焦时再画一条 accent 横线）→ 内嵌 `tk.Text`（bd=0）；聚焦用 `<FocusIn>/<FocusOut>` 切标志位重绘，蓝线两端内缩一个圆角半径，画外观的 item 要 `canvas.tag_lower("chrome")`。
  坑：**必须显式覆写 `config/configure/cget`**——这两个方法在 Frame 上本来就存在，`__getattr__` 转发不生效，否则 `area.cget("bg")` 拿到外层 Frame 的底色、`area.config(fg=)` 报 `unknown option "-fg"`。覆写后转发到内部 Text，`except TclError` 再回退 Frame。
- `theme.PALETTES` 增加 `accent`（浅 #0067c0 / 深 #60cdff）。

**输入/按钮/输出移入标签页**（"放到与 是否允许长度缩短 同级的框里"）
- `_build_io_panel(tab, with_input)`：每页各一套「输入JSON / 转换·复制结果·清空 / 输出JSON」；`self.tab_io[key]` 存面板，`text_input`/`text_output`/`input_frame`/`frame_btn`/`btn_convert|copy|clear` 改为**属性**返回"当前页那一份"（原代码与测试无需改动），新增 `all_text_areas()`/`all_buttons()` 供换主题时全量套色。
- 图片转音符画 / MIDI BPM 两页：输入区**照建但不 pack**（`winfo_manager()` 为 ""），结构一致、断言隐藏状态也天然成立。
- 附带好处：每个功能各自保留输入输出，切页不串内容（清空只清当前页）。
- `on_function_change` 里原来的输入框显隐逻辑删掉（建页时已决定）。

**字体**
- `_get_available_font_family` 改为「区域 SC→HC→CL→TC→J→K × 变体 Mono→Fixed→Term→Gothic→UI」生成候选，本机取到 **Sarasa Fixed SC**。用户装的更纱黑体是 `Sarasa-SuperTTC.ttc`，在 `%LOCALAPPDATA%\Microsoft\Windows\Fonts`（不在 C:\Windows\Fonts）；该 TTC 的简体只有 Fixed/Term，没有 Mono SC / Gothic SC。
- 坑：家族名含空格时 `font=("Sarasa Fixed SC", 10)` 会被 Tk 当 Tcl 列表解析 → `expected integer but got "Fixed"`；新增 `font_spec(size)` 返回 `"{家族} 字号"`，所有 `font=` 与 `option_add` 都改用它。
- 标签栏字号 9（正文 10），8 个标签在 150% DPI 下一行排得下。

**验证**：新增 `tests/_test_req16.py`（33 项：每页一套 IO / 各页独立（含清空只影响当前页）/ 页内顺序 / 圆角+聚焦蓝线的 canvas 断言 / 标签字号 / 字体族与等宽比例）；`_test_req15.py` 的字体段与描边段按新实现改写（样式字体解析改用 `tkfont.Font(font=...).actual()`，描边改查 `area.border_color`）。全套 10 个测试文件共 247 项断言全绿。

## 收尾：提示词历史补齐 14/15/16 轮 + 身份档案落定

- `docs/ai提示词历史.txt` 追加第十四/十五/十六轮（用户原话逐字保留，沿用中文数字分隔行格式），现在共 16 轮。
- `docs/ai提示词历史.md`：用 `prompt-history-to-md` 的 convert.py 重新生成后**只取 14/15/16 三节追加**（避免整份重生成丢掉此前手工润色的 easingType 表与贝塞尔字段表），目录补 3 项（GFM 锚点同规则）。校验：16 个轮次标题、16 条目录、easingType/贝塞尔表仍在、22 段 JSON 代码块不变。
- README 与 memory/MEMORY.md 同步需求十六的约定（标签页内含 IO、RoundedTextArea、font_spec 花括号、Sarasa Fixed SC）。
- 建立身份档案：`~/.workbuddy/IDENTITY.md`（自定名"墨/Mo"）、`USER.md`（handle tzh654321 来自 git 署名，未编造真名/城市）、`SOUL.md` 补工作约定；`BOOTSTRAP.md` 已删除。

