# RPET 工具箱 — 上下文备忘（重要）

> 本文件用于在会话丢失/新对话时快速恢复上下文。核心代码与需求文档位于：
> - 核心代码：`C:\Users\ZETTAKIT\Desktop\temp\RPET new\`（模块化包 `rpe_toolbox/`，入口 `RPET.py`）
> - 需求文档：`C:\Users\ZETTAKIT\Desktop\temp\RPET new\docs\ai提示词历史.txt`
>   - "十十十十十十十十"标记（第 1907 行）之后是最新一批需求（需求十）
> - 矫正染色算法说明：`C:\Users\ZETTAKIT\Desktop\temp\RPET new\Other File\染色矫正.py`（正片叠底/最接近反推）
> - 旧版单文件仍保留在 `C:\Users\ZETTAKIT\Desktop\temp\RPET\RPET.py`（未改动，作为原仓库）

## 一、项目概况
- 软件名：**rpe工具箱**（tkinter GUI，需求九后已拆分为模块化包）
- 目录结构：`rpe_toolbox/{launcher,imglib,easing,core,resources,app}.py`；媒体文件在 `assets/{audio,icons,images}`；注释与备忘在 `Other File/`
- 功能列表（combo 标签 → func_key）：
  1. `hold/事件首尾相接` → hold_notes_connect
  2. `非线性切割` → nonlinear_split
  3. `极坐标转换` → polar_conversion
  4. `事件类型转换` → event_type_convert
  5. `图片转音符画` → image_to_notes
  6. `时间间隔转y偏移` → time_interval_to_yoffset
  7. `倒序/拉伸` → reverse_data
  8. `MIDI BPM 提取` → midi_bpm_extract
- 窗口 1000x760；`relaunch_as_pythonw()` 会以 pythonw 无窗口重启（可用环境变量 `RPET_NO_RELAUNCH=1` 跳过）
- 入口 `RPET.py` 的 `main()` 为 `root = tk.Tk(); app = RPEToolbox(root); root.mainloop()`

## 二、最新一批需求（"八八八"之后，均已实现 ✅）
1. **倒序拉伸事件负值适配**：`func_reverse_data`（~L804）+ 新增 `_reverse_event_fields`（~L858）
   - 负比例时事件 startTime>endTime → 交换 start/end 时间；交换 start/end 值
   - in/out 缓动互换映射：`{2:3,3:2,4:5,5:4,8:9,9:8,10:11,11:10,14:15,15:14,16:17,17:16,18:19,19:18,20:21,21:20,24:25,25:24,26:27,27:26}`
   - easingLeft/easingRight：`new_left = 1 - old_right`、`new_right = 1 - old_left`（先交换再被1减）
   - bezier==1 时 bezierPoints `[x1,y1,x2,y2] → [1-x2, 1-y2, 1-x1, 1-y1]`
   - notes 负比例时：yOffset 取反 + hold 的 start/end 时间若倒置则交换
2. **需求七区分轨道修复**：阈值 270→**175**（`_group_notes_by_track` ~L1207）
   - `_connect_group_notes`（~L1227）：组内按时间排序，仅当下一根 hold 的 |dx|<175 才连接；双押（同 startTime）共同参考下一个不同开始时间
   - `_connect_group_events`（~L1249）：组内按 line 分组，endTime = 下一个不同 startTime
3. **移除"已复制到剪贴板"会话框**：`copy_result`（~L930）已删除 messagebox.showinfo（保留"没有有效结果"警告）
4. **图片转音符画 / MIDI BPM 提取不显示"输入JSON"及输入框**：`on_function_change`（~L447）中对 `image_to_notes`/`midi_bpm_extract` 执行 `input_frame.pack_forget()`；恢复时 `pack(before=self.frame_btn)`（frame_btn 已是 self 属性）
5. **图片转音符画设置项 5 行布局**（`create_widgets` ~L234）：
   - 第1行：图片文件路径
   - 第2行：音符类型 | 颜色处理 | **旧版染色标签**（`legacy_tint_var`）
   - 第3行：音符间隔(分音) | 左右翻转 | 上下翻转 | 旋转度数（密度输入移入此行，`frame_density` 在图片模式下不再显示）
   - 第4行：是否使用原图片大小 (x像素数 y像素数 锁定宽高比)
   - 第5行：是否自动调整音符宽度 (音符宽度)
   - `_toggle_image_size_fields`/`_toggle_image_width_fields` 改用 `side=LEFT` 方式
6. **染色键名默认 `"tint"`**：`func_image_to_notes`（~L755）`color_key = "color" if legacy_tint_var else "tint"`；勾选"旧版染色标签"→`"color"`。对三种颜色模式统一生效（用户明确：染色/矫正颜色染色无论哪种模式，键名默认 tint）
7. **修复音符宽度模式下 x 全在最左侧 bug**（`func_image_to_notes` ~L718）：
   - 自动调整宽度：`step_x = 1350/width`，`pos_x = -675 + (x+0.5)*step_x`（铺满屏幕）
   - 不调整宽度：`step_x = (1350 - note_width)/(width-1)`，`pos_x = -675 + note_width/2 + x*step_x`（与示例 -587.5/0/587.5 一致）
8. **需求九（"九九九九"之后）已实现 ✅**：
   - 拆分文件并构建新目录：单文件 → `rpe_toolbox/` 包；媒体分类到 `assets/`；大段注释（非线性切割/极坐标转换/float_to_time）移入 `Other File/大段注释整理.md`，本备忘也位于 `Other File/`
   - MIDI BPM 提取输出 `BPMList`（`{"bpm": 120.0, "startTime": [0,0,1]}`），无 `midiPath`
   - 自动调整音符宽度：`size = 列间距/175`（如 3 列 → 450/175≈2.5714）；非自动模式 `size = 输入音符宽度/175`（175→1.0，350→2.0）
   - 上下翻转以其反义生成：默认不勾选 → 图片翻转（底部行最早）；勾选 → 不翻转（顶部行最早）
   - 生成 `assets/images/_test_img_alpha.png`（4x4，透明度含 0/32/64/127/191/255）
9. **需求十（"十十十十"之后）已实现 ✅**：
   - 工作区根目录为 `C:\Users\ZETTAKIT\Desktop\temp\RPET new`
   - 包名 `rpet_toolbox` → **`rpe_toolbox`**（入口 RPET.py、tests、README、本备忘均已同步）
   - 需求文档更名：`使用python和tkinter制作以下音游相关功能.txt` → `docs/ai提示词历史.txt`（docs 中仅保留新名字）
   - 图片转音符画透明度修复：所有颜色模式下像素透明度直接写入音符 `alpha`（此前除 0 外都被写成 255）；0 透明度像素仍跳过；半透明蓝 → `{"alpha": 127, "tint": [0,0,255]}`
   - 自动调整音符宽度确认生效：`size = 列间距/175`（3 列 → 2.5714），已由测试覆盖
10. **启动闪退修复（重要）**：`relaunch_as_pythonw` 原用自身 `__file__`，拆分后误把 `rpe_toolbox/launcher.py` 当入口脚本重启 → 瞬间退出。现已改为显式传入入口 `RPET.py`（`relaunch_as_pythonw(script_path=os.path.abspath(__file__))`），并新增启动异常日志 `Other File/启动错误.log`（pythonw 无控制台，出错时可查此文件）
11. **需求十一（"十一十一"之后，`ai提示词历史.txt` 第 1938 行起）已实现 ✅**：
    - 图片转音符画时间间隔修正：每行间隔 = 音符间隔（16分音 → 1/4 拍），不再按行数等比压缩（`func_image_to_notes` 改用 `start_beat = y * step_beat`）
    - 非线性切割三次贝塞尔：新增 `_subdivide_bezier`（`easing.py`，de Casteljau 细分 + 归一化），每个子段按数学公式推导控制点，不再复制整条曲线的控制点
    - 事件类型转换 UI 重构（`app.py _create_event_convert_rows`）：两列布局，每行“输入选择框 → 文字”；速度更名流速；左列 X轴位移/Y轴位移/旋转/透明度/流速，右列 X轴缩放/Y轴缩放/定轨hold/曲线drag/音符间隔（输入框，仅曲线drag非“无”时显示）
    - 定轨hold（`_events_to_hold_notes`）：5k 轨道 -540/-270/0/270/540；7k 轨道 -578.57/-385.71/-192.86/0/192.86/385.71/578.57；事件按类型 1..k 映射为 hold
    - 曲线drag（`_events_to_drag_notes`）：x→type1/type6、y→type2/type7，按音符间隔采样生成 drag（type4），size/judgeArea 取缩放值，无缩放覆盖时取 1.0；相同时间+位置去重
    - 测试：`tests/_test_req11.py`（17 项：图片间隔、定轨hold 5k/7k、曲线drag 与文档数值一致、贝塞尔子段）
12. **需求十二（"十二十二"之后，`ai提示词历史.txt` 第 2287 行起）已实现 ✅**：
    - 非线性切割贝塞尔优化：de Casteljau 几何细分的子段 y 控制点会跑到 [0,1] 外（如 -1.744/1.73），游戏内渲染大幅过冲，组合效果与原曲线偏差最大 162（量程 900）。`_subdivide_bezier`（`easing.py`）改为：保留几何细分的 x 时间映射 + 对 y 控制点做最小二乘拟合（21 点采样），组合效果最坏偏差降到 <3（≈0.3%）；段边界值仍精确
    - 事件类型转换：输入选择框与文字之间加入“→ ”（含定轨hold/曲线drag/音符间隔行）
    - 矫正颜色染色修复（`core.py _corrected_color_from_rgb`）：原实现是正向相乘（tint=像素×固定色），渲染后变成像素×固定色² 更偏离；现按 `Other File/染色矫正.py` 的 `multiply_reverse_nearest` 反推最接近结果：目标≥固定色→255，否则 round(目标×255/固定色)
    - 颜色处理为“不透明度替代亮度”时隐藏“旧版染色标签”勾选框（`app.py _toggle_legacy_tint`）
    - 测试：`tests/_test_req12.py`（10 项：组合效果偏差、反推染色、UI 显隐与箭头）
13. **打包与图标（最新）已实现 ✅**：
    - 窗口标题栏图标：优先使用 `assets/icons/mini ico-z1.png`（16x16），`resources.find_icon()` 已调整顺序；完整版图标由 `resources.find_full_icon()` 提供（exe 打包用）
    - `resources.py` 已支持 PyInstaller 冻结环境（`sys._MEIPASS` 下解析 assets）
    - 打包脚本：`C:\Users\ZETTAKIT\Desktop\temp\RPET\build_exe.py`（旧目录，按用户要求）；同时在 `RPET new\build_exe.py` 放了一份，两个位置均可运行（自动定位项目）
    - 打包格式：PyInstaller `--onefile --windowed`，exe 图标由 `ico-z1.png` 转为多尺寸 .ico，`--add-data assets;assets` 囊括全部媒体文件（audio/icons/images）
    - 产物：`RPET new\dist\rpe工具箱.exe`（约 17.9 MB）；启动正常
    - 注意：重新打包前需确保没有正在运行的 rpe工具箱.exe（onefile 会锁文件导致 PermissionError）
14. **打包文件迁移与 Tcl/Tk 9 修复（最新）✅**：
    - 所有打包相关文件已迁移到旧目录 `C:\Users\ZETTAKIT\Desktop\temp\RPET`：`build_exe.py`、`build/`（含 ico-z1.ico、tcl/tk 解压数据、PyInstaller work）、`dist/rpe工具箱.exe`、`rpe工具箱.spec`；`RPET new` 只保留源码
    - `build_exe.py` 已同步修改：`--specpath/--workpath/--distpath` 全部指向旧目录，产物 `RPET\dist\rpe工具箱.exe`
    - Tcl/Tk 9 打包修复：Python 3.15 的 Tcl 9 数据在 `libtcl9.0.3.zip`/`libtk9.0.3.zip`（zipfs）内，PyInstaller 无法自动收集，导致运行时 `Tcl data directory ... _tcl_data not found`。脚本现在打包前解压两个 zip，经 `--add-data` 映射为 `_tcl_data`/`_tk_data`
    - 注意：tk zip 判断用文件名（`libtcl` 前缀）而非路径包含，否则父目录名 tcl 会误判；`libtk9.0.3.zip` 本身没有 init.tcl（Tk 9 已内嵌初始化），属正常
    - 实测：打包后 exe（约 19.7 MB）启动正常，无 Tcl 报错
15. **任务栏图标与 exe 图标一致（最新）✅**：
    - 原因：tkinter `iconphoto` 只设置了 16x16 小图标，任务栏用的是窗口大图标，显示模糊且与 exe 图标不一致
    - 修复：`app.py _apply_taskbar_icon()` 用 ctypes `LoadImageW` + `WM_SETICON` 分别设置标题栏小图标（16x16）与任务栏大图标（32x32），两者都取自 `ico-z1.png`（与 exe 资源图标同源）；临时 .ico 由 Pillow 内存生成、用后删除
    - 已验证：`WM_GETICON` 读回句柄与设置句柄完全一致；打包后 exe（约 19.8 MB）启动正常
16. **任务栏图标仍模糊 + 临时目录清理警告（最新）✅**：
    - 任务栏图标修复升级（`app.py _apply_taskbar_icon` 三管齐下）：
      1. 打包环境（frozen）`SetCurrentProcessExplicitAppUserModelID("rpetoolbox")`，任务栏直接使用 exe 资源图标（与 exe 一致）
      2. `WM_SETICON` 设置窗口小/大图标（16x16 / 32x32，均取自 ico-z1.png）
      3. `SetClassLongW` 设置类图标（GCLP_HICON=-14 / GCLP_HICONSM=-34），覆盖任务栏/Alt-Tab 读取类图标的路径
    - 图标在 `__init__`、`after(300)`、`<Map>` 三处应用（避免 Tk 显示阶段覆盖），每次调用先 `DestroyIcon` 释放旧句柄
    - 临时目录清理警告缓解：新增 `app.shutdown()`（销毁 Tk + join 音频线程），`RPET.py main` 的 `finally` 调用；实测正常关闭后 `Temp\_MEI*` 无新增残留
    - 历史残留 `_MEI*` 目录已清理 7 个；`_MEI89322`（7/27）因 `libffi-8.dll` 被占用未删（可能有其他进程在用）
17. **任务栏图标最终修复（重要）✅**：
    - 根因：Tk 在 Windows 上有多个窗口——`winfo_id()` 返回 TkChild（隐藏），任务栏看到的是 **TkTopLevel**；且 `iconphoto` 会把类图标（任务栏读取）设成 16x16 mini 并在窗口显示时覆盖手动设置
    - 修复（`app.py _apply_taskbar_icon`）：
      1. 用 Tk 官方命令 `wm frame .` 获取 TkTopLevel 句柄（可靠，不依赖枚举）
      2. `SetClassLongW(TkTopLevel, GCLP_HICON=-14, 32x32)` 设置任务栏/Alt-Tab 大图标（取自 ico-z1.png，与 exe 图标同源）；`GCLP_HICONSM=-34` 设置标题栏 16x16
      3. 移除 `iconphoto`（避免 Tk 覆盖类图标）；frozen 下保留 AppUserModelID 绑定 exe 图标
      4. 窗口显示后（`after(300)`/`<Map>`）再应用，找不到句柄时每 500ms 重试（最多 10 次）
    - 已验证：exe 运行后 TkTopLevel 类图标 big=(32,32)、small=(16,16)，VERIFY PASS；正常关闭无新增 `_MEI*` 残留
18. **羽毛图标回归与最终方案（最新）✅**：
    - 现象：移除 iconphoto 后窗口显示 Tk 默认羽毛；加回 iconphoto 后 25 秒类图标又被 Tk 覆盖回 16x16（iconphoto 是覆盖源）
    - 最终方案：**不使用 iconphoto**，完全用 `wm frame` 句柄 + `SetClassLongW` 设置类图标（big 32x32 / small 16x16），并加 `_keep_taskbar_icon` 定时器（每 3 秒检查，类图标被重置时重新应用）
    - 已验证：exe 运行 25 秒后类图标仍 big=(32,32)、small=(16,16)，VERIFY PASS；正常关闭无 `_MEI*` 残留；81 项测试全绿
19. **任务栏羽毛根因与修复（最终，重要）✅**：
    - 真正根因：`tk.Tk()` 创建即显示，任务栏按钮在窗口首次显示时**快照类图标**（Tk 默认羽毛）；之后无论怎么改类图标都不会刷新已创建的任务栏按钮（此前验证只查了类图标当前值，误判成功）
    - 修复（`app.py __init__`）：**先 `root.withdraw()` 隐藏 → 同步设置类图标（wm frame + SetClassLongW，32x32/16x16）→ 再 `root.deiconify()` 显示**。类图标是类级别的，Tk 显示时重建窗口后仍继承 32x32；任务栏按钮创建时读到正确图标
    - 保留 `_keep_taskbar_icon` 定时器（每 3 秒检查纠正）与 frozen 下 AppUserModelID
    - 临时目录清理加固：`_play_button_sound` 播放完成后 `pygame.mixer.quit()`（避免持有 _MEIPASS 音频句柄）+ `shutdown()` join 线程
    - 已验证：exe 运行后类图标 big=(32,32)、small=(16,16)，正常关闭无新增 `_MEI*` 残留
20. **任务栏图标终极修复：子类化 WM_GETICON（最新，重点）✅**：
    - 为什么类图标方案在用户环境无效：Explorer 可能缓存 TkTopLevel 窗口类的图标（羽毛），改类图标不刷新任务栏；且此前验证只查类图标当前值，未覆盖 Explorer 缓存路径
    - 终极方案（`app.py _ensure_icon_subclass`）：**子类化 TkTopLevel 窗口过程**，拦截 `WM_GETICON` 动态返回我们加载的 32x32/16x16 图标。任务栏查询窗口图标是动态调用（不走缓存），必定拿到 ico-z1
    - 实现要点：`SetWindowLongW(GWL_WNDPROC)` 替换窗口过程，回调中 `WM_GETICON` 直接返回 `self._icon_big/_icon_small`，其余消息调用旧过程（`ctypes.cast` 函数指针直调，避免 CallWindowProcW 32 位问题）；回调引用保存在 `self._icon_wndproc_refs` 防 GC；窗口重建（deiconify 后句柄变化）时重新子类化
    - 前提：`__init__` 中 `withdraw() → update() → 设置图标/子类化 → deiconify()`（update 确保句柄与标题就绪）
    - 已验证（跨进程，即任务栏视角）：源码与打包 exe 运行中，独立进程查询窗口 `WM_GETICON` 均返回 big=(32,32)、small=(16,16)
21. **标题栏大图标修正（最新）✅**：
    - 现象：标题栏与任务栏都显示 32x32 大图标——窗口创建时标题栏快照了类大图标（Tk 类未提供 small 图标时标题栏 fallback 用 big）
    - 修复：子类化回调拦截 `WM_SETICON` 并转发 `DefWindowProc`（存储图标并触发系统刷新），窗口设置完图标后主动 `SendMessage WM_SETICON(ICON_SMALL=0, 16x16)` 刷新标题栏、`WM_SETICON(ICON_BIG=1, 32x32)` 刷新任务栏
    - 已验证：exe 运行后 `WM_GETICON` small=(16,16)、big=(32,32)、small2=(16,16)；81 项测试全绿
22. **标题栏改用 mini ico-z1.png（最新）✅**：
    - 现象：标题栏显示的是 ico-z1.png 的 16x16 缩小帧，而非专用 `mini ico-z1.png`
    - 修复：`_apply_taskbar_icon` 拆分两个图标源——标题栏小图标从 `mini ico-z1.png` 生成 16x16 ICO 加载（缺失时回退完整版），任务栏大图标仍从 `ico-z1.png` 生成 32x32 加载
    - 已验证：exe 运行后 `WM_GETICON` small=(16,16)、big=(32,32)；81 项测试全绿

## 三、关键实现细节 / 注意事项
- 新目录内源码统一 UTF-8；原单文件为 CRLF（若再改旧文件需注意行尾）
- `time_to_float([a,b,c]) = a + b/c`；`float_to_time` 用分母≤192 的最简分数表示
- `apply_easing`（`easing.py`）支持 1-29 缓动类型 + bezier；`interpolate_value` 同文件
- 图片示例（3x3、不调宽度、染色、16分音、drag）期望输出：x 列坐标为 -587.5 / 0 / 587.5，底部行 startTime 最早
- `_resolve_image_target_size`（`core.py`）：use_original_size=False 时按 x/y 像素数缩放；锁定宽高比会同步另一维
- 音频/图标路径由 `resources.py` 解析到 `assets/audio`、`assets/icons`

## 四、测试
- 功能测试：`tests/_test_rpet.py`（23 项全过：倒序 events/notes、图片 x 分布与 size、tint 键、hold 区分轨道与双押）
- UI 测试：`tests/_test_ui.py`（12 项全过：8 功能切换无异常、输入框隐藏/显示、图片模式 process_data）
- 需求九/十验证：`tests/_test_req9.py`（13 项全过：BPMList、音符宽度 size、上下翻转反义、多透明度 PNG、alpha 写入）
- 运行：`python tests/_test_rpet.py` 等（会创建隐藏 Tk 窗口，需有图形环境）
- 语法检查：`python -m py_compile RPET.py rpe_toolbox\*.py`（通过）

## 五、环境/其他
- 旧目录 RPET.exe 是打包产物（RPET.spec，PyInstaller）；新目录如需打包可参照旧 RPET.spec
- 需求文档中"四四四四"段落之前是旧需求（如 allow_shorten_var 尚未实现/未使用），本轮未涉及
- 图片时间顺序（上→下 vs 下→上）不在"八八"后需求范围内，未改动
- 用户偏好：重要思考内容会定期写入本文件，供新对话直接使用
