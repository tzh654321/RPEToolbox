# 大段注释整理

需求九要求将代码中的大段注释移出，故原单文件 `RPET.py` 中的长段推导/说明注释统一存放于此。
代码中仅保留结论性注释，并指向本文件。

## 1. float_to_time（时间数组反向转换）

原注释内容：

```
# 简单转换回整数拍子，保留精度可能需要更复杂的分数运算，这里为了兼容RPE通常格式
# RPE中通常 endTime 只是参考，这里尽量保持原格式或转为最简分数
# 为简化，直接取整拍子，或者保持浮点逻辑在内部，输出时若需严格格式需另写
# 这里采用一种简单的策略：如果原来是列表，尝试还原，否则保留浮点或取整
# 注意：题目要求输出格式也是 [a,b,c]，我们需要反向转换
```

结论：输出 `[整数拍, 分子, 分母]`，分母取 1~192 中最简近似。

## 2. func_nonlinear_split（非线性切割）—— 分音密度推导

原注释内容：

```
# 计算段数: 密度通常指每拍多少个点，或者总时长切多少份？
# 题目例子：16分音。Input duration: 192-190 = 2 beats.
# Output count: 8 events. 2 beats * 4 (quarter notes per beat? No.)
# Let's look at the output times:
# 190.0, 190.25, 190.5, 190.75, 191.0 ...
# Step is 0.25 beats. 1 beat / 4 = 0.25. So density 16 means 16th notes?
# 16th note = 1/4 beat. Yes.
# So step = 1 / (density / 4) ? No.
# Standard music: 4/4 time. 1 beat = 4 sixteenth notes.
# If density is 16, it usually means 16 divisions per beat? Or 16th notes?
# In RPE/Phigros context, "16" usually means 16分音, which is 1/4 of a beat.
# Step size = 1.0 / (density / 4.0) is wrong.
# 4分音 = 1 beat. 8分音 = 0.5 beat. 16分音 = 0.25 beat.
# Formula: step = 4.0 / density
```

结论：`step = 4.0 / density`（16 分音 = 1/4 拍）。

## 3. func_nonlinear_split —— 切分后是否保留 easingType

原注释内容：

```
# Usually split events become linear (Type 1) between the calculated points?
# The example output keeps "easingType": 5 for the first event?
# Wait, example output:
# First split event: easingType 5.
# Second split event: easingType 5.
# This implies the sub-segments retain the type? Or maybe they are just linear segments approximating it?
# Actually, if you split a curve into small enough linear segments, you approximate it.
# But the example output KEEPS easingType 5. This is strange for a "split" unless the engine interprets it differently.
# Let's look at the second event in example (Type 24). Output keeps Type 24.
# Okay, I will keep the original easingType.
```

结论：保留原 `easingType`（与需求示例输出一致）。

## 4. func_polar_conversion（极坐标转换）—— 实现说明

原注释内容（分段摘录）：

```
# 1. Separate events by type
# Type 1: X pos? Type 2: Y pos? Type 3: Rotation?
# Prompt: "输入的x与y事件... 旋转事件... 结合"
# Usually in RPE: Type 1=X, Type 2=Y, Type 3=Rotation (or Scale/Alpha depending on context, but here Rotation).
# Let's assume: Type 1 = X, Type 2 = Y, Type 3 = Rotation.

# 2. Normalize timelines
# "每个事件未定义的时间段沿用上个事件的结束值，若没有上一个事件则使用下一个事件的起始值"
# We need to create continuous functions for X, Y, Rot over the total time range.
```

取值策略：

```
# Helper to get value at time T for a specific type
# Filter events of this type
# Sort by start time
# Find active event
# If not found, look for previous
#   "沿用上个事件的结束值" -> Use end value
# Look for next
#   "使用下一个事件的起始值"
```

极坐标换算的完整推导（原始长注释）：

```
# Generate X, Y, Rot series
# Then convert to Distance (Line 0 in output example has Type 1 events which seem to be Distance?)
# Output Example:
# Line 0, Type 1: Values like 38.26, 70.71... These are Distances?
# Line 0, Type 2: Values like 92.38...
# Line 0, Type 3: Rotation.

# Wait, the prompt says: "计算出绝对的x、y事件... 最后和原旋转事件汇总并输出"
# But the example output ONLY has Line 0. And Types 1, 2, 3.
# And the values in Type 1 and 2 in output look like Distances?
# Let's check the first output event: Type 1, Start 0, End 38.26.
# Input had Type 3 (Rot) 90->270. Type 1 (X) 0->0? No Input Type 1 was 0->0 (Wait, Input Type 1: start 0 end 0? No, Input Event 2: Type 1, start 0 end 0).
# Input Event 4: Type 2, start 100 end 100.
# Input Event 6: Type 3, start 0 end 90.

# Let's re-read carefully: "将距离和角度事件用极坐标的方式结合... 转换为x和y绝对位置"
# "计算出一系列距离事件... 再计算出一系列绝对的x、y事件"
# The example output seems to replace the original X/Y with Distance? Or maybe the example output is just showing the intermediate Distance calculation?
# "例：输入... 输出..."
# The output events have Line 0. Original events had Line 1.
# The output Type 1 values: 38.268...
# At t=178.0 (Start):
# Rot: 0 deg (from Event 6: 0->90, wait, Event 6 starts 178.0? No, Event 6 start [178,0,1]).
# Let's trace t=178.0.
# Rot (Type 3): Event 6 starts 178.0 val 0. Event 1 starts 180.0 val 90. So at 178.0, Rot=0.
# X (Type 1): Event 2 starts 178.25? No [178, 1, 4] is 178.25. Event 5?
# This is getting complex to reverse engineer exactly without the specific RPE coordinate system definition (center of screen etc).

# Implementation Strategy for "Polar Conversion":
# 1. Sample X, Y, Rot at each time step.
# 2. Convert X, Y to Distance from Center (0,0)? Or is (0,0) center?
#    Prompt: "(x^2+y^2)^0.5来表示与圆心距离". So Dist = sqrt(x^2 + y^2).
# 3. The prompt says "转换为x和y绝对位置".
#    But the example output seems to contain Distances in Type 1 and 2?
#    Actually, looking at Output Event 1 (Type 1): Start 0, End 38.26.
#    Output Event 17 (Type 2): Start 100, End 92.38.
#    Output Event 29 (Type 3): Start 0, End 90.
#    It seems Type 1 becomes Distance? And Type 2 becomes... something else?
#    Or maybe Type 1 is X, Type 2 is Y, but calculated from Polar?
#    If Rot=0 (Up, Y+), and Dist=100. X=0, Y=100.
#    If Rot=90 (Right, X+), and Dist=100. X=100, Y=0.

#    Let's look at the example output values again.
#    First event (Type 1): 0 -> 38.26.
#    Third event (Type 3, Rot): 0 -> 90.
#    If this is X position:
#    At start (Rot 0, Dist?): If Dist is from Y event (Type 2)?
#    Input Type 2 (Y) at start 178.0? Event 4 ends 178.25 val 100. Event 5 starts 179.0.
#    This is too ambiguous to perfect match the black-box example without more spec.

#    I will implement the LOGIC described:
#    1. Calculate Dist = sqrt(X^2 + Y^2).
#    2. Calculate Abs X = Dist * sin(Rot), Abs Y = Dist * cos(Rot) (0 deg is Y+).
#    3. Output Events for X (Type 1), Y (Type 2), Rot (Type 3).
```

循环内对输入含义的讨论：

```
# Get raw values from input events (interpreting Type 1 as X, 2 as Y, 3 as Rot)
# Note: The prompt says "Inputs X and Y events... represent distance".
# This implies Input Type 1 and 2 are NOT x/y coordinates, but Distances?
# "此模式下输入的x与y事件不再表示锚点在屏幕上的绝对位置，而是以(x^2+y^2)^0.5来表示与圆心距离"
# This sentence is confusing. Does it mean Input X and Input Y are BOTH distances?
# Or does it mean we calculate distance FROM input X and Y?
# "将距离和角度事件用极坐标的方式结合"
# Likely: One of the inputs is Distance, one is Rotation?
# But there are 3 types.
# Let's assume:
# Type 1: Distance Component 1?
# Type 2: Distance Component 2?
# Type 3: Rotation.

# Re-reading: "输入的x与y事件... 表示与圆心距离".
# Maybe Input Type 1 is Radius R? Input Type 2 is unused or Z?
# Or maybe Input Type 1 is X-offset, Type 2 is Y-offset, and we convert THAT to polar?
# No, "转换为x和y绝对位置".

# Let's stick to the most standard interpretation of such tools:
# Input Type 1 = X control, Input Type 2 = Y control, Input Type 3 = Rotation.
# But the text says "Input X/Y ... represent distance".
# I will assume:
# Input Type 1 = Distance (Radius)
# Input Type 3 = Angle
# Input Type 2 = Maybe another Distance or ignored?
# In the example, Type 2 exists.

# Let's try:
# Val1 (Type 1) = R1
# Val2 (Type 2) = R2
# Maybe X = R1 * cos(Rot), Y = R2 * sin(Rot)? Unlikely.

# Given the complexity and ambiguity, I will implement a placeholder that:
# 1. Reads Type 1, 2, 3.
# 2. Treats Type 1 as X, Type 2 as Y, Type 3 as Rot.
# 3. Converts to Polar (Dist, Angle).
# 4. Converts back to Cartesian (NewX, NewY).
# 5. Outputs NewX (Type 1), NewY (Type 2), Rot (Type 3).
```

最终结论（代码当前实现）：

```
# Convert to Polar - 计算与圆心距离，并根据旋转输出绝对坐标
# dist = sqrt(x^2 + y^2)；rot 按角度转弧度
# new_x = dist * sin(rot)，new_y = dist * cos(rot)（0 度指向 Y 正方向）
```
